<p style="color: green"><%= notice %></p>
<div align="left">
<%= link_to "New match", new_match_path , class: 'btn btn-xs btn-primary my-1' %>
</div>
<div align="right">
<%= form_tag matches_path, method: :get do %>
  <%= text_field_tag :search, params[:search], placeholder: "Szukaj drużyny" %>
  <%= submit_tag "Szukaj", name: nil %>
<% end %>
</div>
<%= paginate @matches %>

<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">First team</th>
      <th scope="col">First team score</th>
      <th scope="col">Second team score</th>
      <th scope="col">Second team</th>
      <th scope="col">Created at</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    <% @matches.each do |match| %>
        <tr>
          <td><%= match.id %></td>
          <td><%= match.first_team %></td>
          <td><%= match.first_team_score %></td>
          <td><%= match.second_team_score %></td>
          <td><%= match.secod_team %></td>
          <td><%= match.created_at %></td>
          <td>
          <%= link_to 'Show', match_path(match), class: 'btn btn-xs btn-primary my-1' %>
          <%= link_to 'Edit',
                      edit_match_path(match), :class => 'btn btn-default btn-xs' %>
          <%= link_to 'Destroy',
                      match_path(match),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
        </tr>
      </a>
    <%end%>
  </tbody>
</table>